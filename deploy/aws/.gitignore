# Generated by `nova deploy render`. Derived from the tenant bundle, so the bundle is the
# thing to review and to version; a committed copy of this file is a second source of truth
# that will disagree with the first one exactly when it matters.
nova.auto.tfvars.json

# Local state and anything carrying an account id.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
*.auto.tfvars
!terraform.tfvars.example
